{
  "name": "@pramaana/app",
  "private": true,
  "version": "0.1.0",
  "description": "Sybil-resistant airdrop demo built on @pramaana/sdk",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "demo": "tsc && node dist/index.js",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@pramaana/sdk": "workspace:*",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
