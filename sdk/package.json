{
  "name": "@pramaana/sdk",
  "private": true,
  "version": "0.1.0",
  "description": "Pramaana drop-in SDK: enroll() / prove(serviceId) / verifyOnChain()",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
